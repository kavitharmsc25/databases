only queries:

create database kavitha_db;
use kavitha_db;
create table employees(
    empid int,
    empname varchar(50),
    empage int,
    dept varchar(50),
    salary double
);

insert into employees values(101, "abi", 23, "velachery", "developer", 500000);
